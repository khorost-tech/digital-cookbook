# Клиент stream-протокола подключается к 5552, затем брокер сообщает ему хост
# партиции-лидера. Для локального стенда объявляем localhost, иначе клиент получит
# внутренний hostname контейнера и не сможет подключиться (типичная засада за NAT).
stream.advertised_host = localhost
