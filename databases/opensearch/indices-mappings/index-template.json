{
  "index_patterns": ["app-logs-*"],
  "composed_of": ["logs-common"],
  "priority": 100,
  "template": {
    "settings": {
      "number_of_shards": 1,
      "number_of_replicas": 0
    },
    "mappings": {
      "dynamic": "strict"
    }
  }
}
