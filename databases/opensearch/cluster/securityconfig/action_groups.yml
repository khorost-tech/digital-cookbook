---
# action_groups.yml — кастомные группы действий. Здесь пусто (только _meta), но файл
# ОБЯЗАН присутствовать: securityadmin.sh -cd требует все 9 типов конфигурации файлами,
# иначе аварийно завершается (exit 255). Совпадает с дефолтом дистрибутива OpenSearch.

_meta:
  type: "actiongroups"
  config_version: 2
