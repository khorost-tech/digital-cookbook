# Запуск cmd/pgss-top (Go 1.26.3, pgx v5.10.0, 2026-07-22)
# PG_DSN=postgres://postgres:shopdemo@localhost:5434/shopdemo go run .

 calls     total_ms    mean_ms       rows  query
     1        842.6     842.64          4  SELECT o.status, count(*) FROM orders o JOIN order_items oi ON oi.orde
     1        381.0     381.02          4  SELECT status, count(*), avg(total) FROM orders GROUP BY status
     1         15.7      15.71          1  SELECT count(*) FROM products WHERE price > $1
     3          1.3       0.43        122  SELECT * FROM orders WHERE customer_id = $1

# Та же диагностика, что в сц.09, но из кода: так строят мониторинг и алерты по
# «тяжёлым запросам» в проде. pgx читает pg_stat_statements обычным SELECT.
