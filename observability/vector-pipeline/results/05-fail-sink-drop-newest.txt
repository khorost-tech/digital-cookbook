=== режим: drop_newest
=== AGG_CONFIG: /etc/vector/variants/agg-drop-newest.yaml
=== параметры генерации: rate=8000 duration=240s
=== останавливаем OpenSearch
=== генерация при недоступном синке
буфер на диске, КиБ: 131100
=== поднимаем OpenSearch и ждём слива буфера
сгенерировано (сырых строк, nginx+app): 1919920
ожидалось без потерь синка (nginx_expected+app_expected после filter/dedupe): 802696
в индексе:      276722
разница от сырого total: 1643198
потери на синке (expected_no_sink_loss - indexed): 525974
=== внутренние метрики Vector (:9598/metrics), сняты после прогона (стенд ещё не гашен)
mark_aggregator component_received_events_total: 802696
from_kafka component_received_events_total: 802696
opensearch buffer_received_events_total (принято буфером синка): 276722
opensearch buffer_sent_events_total (отправлено из буфера в OpenSearch): 276722
=> буфер синка отказал принять 802696 - 276722 = 525974 события (when_full: drop_newest),
   что совпадает с "потери на синке" выше, посчитанными независимо через _count OpenSearch.

=== ёмкость буфера в байтах (эта строка дописана позже, при подготовке статьи)
Провенанс: значение снято в ЭТОМ ЖЕ прогоне drop_newest вместе с блоком метрик выше,
но в файл тогда не попало — было зафиксировано только в отчёте замера
(.superpowers/sdd/2026-07-27-vector-data-pipeline/task-10-report.md). Переношу сюда,
чтобы число, на которое опирается статья, лежало рядом с остальными числами прогона.
Повторным прогоном НЕ переснималось.

opensearch buffer_received_bytes_total: 134217320

Разбор (осторожно с единицами — это то место, где легко ошибиться):
  134217320 байт            = 128,0 MiB
  граница сегмента 128 MiB  = 134217728 байт
  расхождение               = 408 байт

  Строка "буфер на диске, КиБ: 131100" в шапке этого файла — это КиБ (du -sk),
  единица подписи исправлена (изначально скрипт печатал "КБ", что и создавало
  путаницу):
  131100 КиБ = 134246400 байт = 128,03 MiB. То есть du и метрика Vector дают
  ОДНО И ТО ЖЕ число, а не 131 против 128. Читать вывод du -sk как мегабайты —
  ошибка, из-за которой совпадение выглядит расхождением.

  Потолок сегмента 128 MiB документирован:
  https://vector.dev/docs/architecture/buffering-model/
