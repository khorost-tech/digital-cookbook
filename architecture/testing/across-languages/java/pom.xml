<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>tech.khorost</groupId>
  <artifactId>across-languages-java</artifactId>
  <version>1.0-SNAPSHOT</version>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.12.2</junit.version>
    <jqwik.version>1.9.3</jqwik.version>
    <mockito.version>5.18.0</mockito.version>
    <testcontainers.version>1.21.3</testcontainers.version>
    <postgresql.version>42.7.7</postgresql.version>
    <!-- Переопределяется из CLI: -Dexcluded.groups= (пусто = гонять всё) -->
    <excluded.groups>integration,property-demo</excluded.groups>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>${postgresql.version}</version>
    </dependency>

    <!-- ПРИЁМ 1: табличный тест — @ParameterizedTest -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- ПРИЁМ 2: property-based со шринкингом -->
    <dependency>
      <groupId>net.jqwik</groupId>
      <artifactId>jqwik</artifactId>
      <version>${jqwik.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- ПРИЁМ 3: моки — идиома Java -->
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>${mockito.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- ПРИЁМ 4: настоящий Postgres в контейнере -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- Без биндинга SLF4J Testcontainers молча проглатывает диагностику:
         «Could not find a valid Docker environment. Please see logs» — и логов нет. -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>2.0.17</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Даёт ${org.mockito:mockito-core:jar} — путь к jar'у для -javaagent ниже. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.8.1</version>
        <executions>
          <execution>
            <goals>
              <goal>properties</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.6</version>
        <configuration>
          <!-- Два обязательных флага, каждый лечит свою болячку:
               1) -javaagent для Mockito: без него он цепляет агент к самому себе
                  на ходу. Сейчас это работает с предупреждением, но JDK уже
                  обещал запретить динамическую загрузку агентов по умолчанию.
               2) -Dapi.version: docker-java по умолчанию говорит с демоном на
                  API 1.32, а Docker 29 поддержку всего, что ниже 1.40, убрал —
                  без этого флага Testcontainers на Docker 29 не стартует вовсе
                  («client version 1.32 is too old»). 1.40 понимают и старые
                  демоны (с Docker 19.03), так что флаг безопасен. -->
          <argLine>-javaagent:${org.mockito:mockito-core:jar} -Dapi.version=1.40</argLine>
          <!-- Интеграционные тесты помечены @Tag("integration") и по умолчанию
               не запускаются: они требуют Docker и стоят секунды, а не мсек.
               Запуск: mvn test -Dgroups=integration -->
          <excludedGroups>${excluded.groups}</excludedGroups>
          <includes>
            <include>**/*Test.java</include>
            <include>**/*Properties.java</include>
          </includes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
