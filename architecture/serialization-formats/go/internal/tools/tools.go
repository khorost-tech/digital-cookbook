//go:build tools

// Пакет tools существует только затем, чтобы у go.mod был РЕАЛЬНЫЙ
// (а не только упомянутый в тексте отчёта) повод держать версию
// github.com/klauspost/compress — она понадобится будущей задаче
// (измерение сжатых размеров кадра), а `go mod tidy` без единого
// настоящего импорта тихо вычёркивает из go.mod всё неиспользуемое —
// это уже случилось в круге правок 1 (см. отчёт). Тег build "tools"
// исключает файл из обычной сборки: он существует только для
// `go mod tidy`/`go list -m`, ни один бинарник его не увидит.
package tools

import (
	_ "github.com/klauspost/compress/zstd"
)
