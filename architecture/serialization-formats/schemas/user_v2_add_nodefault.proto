syntax = "proto3";
package tech.khorost.serialization;

// В proto3 обязательных полей не существует, поэтому "нет значения по
// умолчанию" здесь нечем отличить от add_default: файл идентичен ему.
message User {
  int64 id = 1;
  string name = 2;
  string email = 3;
  optional int32 age = 4;
}
