# Сборка использует закешированные метаданные запросов (.sqlx/), поэтому cargo build проходит
# БЕЗ живой БД. Чтобы перегенерировать кеш под изменённые запросы — поднимите Postgres,
# выставьте DATABASE_URL и запустите `cargo sqlx prepare` (см. README).
[env]
SQLX_OFFLINE = "true"
