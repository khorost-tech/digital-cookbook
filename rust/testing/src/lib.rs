//! Стенд к статье «Тестирование в Rust».
//!
//! Шесть приёмов на одном домене (объёмная скидка по тирам — тот же, что в
//! стенде architecture/testing/across-languages для Go/Java/Python):
//!
//! 1. юниты в `#[cfg(test)] mod tests` рядом с кодом — видят приватное;
//! 2. интеграционные в `tests/` — чёрный ящик, приватного не видят;
//! 3. doc-тесты — примеры в документации, которые компилируются и запускаются
//!    (включая `compile_fail`: проверка, что неверный код НЕ собирается);
//! 4. property-based на proptest — генератор, инвариант, шринкинг;
//! 5. дублёры: mockall против фейка руками;
//! 6. Testcontainers: настоящий Postgres.
//!
//! Запуск: см. README.md рядом.

pub mod money;
pub mod pricing;
pub mod service;
