---
# Handlers для роли compose_stack

- name: Перезапустить стек
  community.docker.docker_compose_v2:
    project_src: "{{ deploy_dir }}"
    project_name: "{{ compose_project }}"
    state: present
    pull: missing
